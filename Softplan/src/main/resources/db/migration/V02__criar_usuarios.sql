CREATE TABLE IF NOT EXISTS testdb.usuario (
    id BIGINT(20)  generated by default as identity PRIMARY KEY,
    nome VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL,
    senha VARCHAR(150) NOT NULL,
    ativo BOOLEAN NOT NULL DEFAULT TRUE
);



INSERT INTO testdb.usuario (nome, email, senha) VALUES ('ADMINISTRADOR', 'admin@softplan.com', '123' );
INSERT INTO testdb.usuario (nome, email, senha) VALUES ('TRIADOR', 'triador@softplan.com', '123' );
INSERT INTO testdb.usuario (nome, email, senha) VALUES ('FINALIZADOR', 'finalizador@softplan.com', '123' );
